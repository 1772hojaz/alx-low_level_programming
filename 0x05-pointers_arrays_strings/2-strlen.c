#include <string.h>
/**
 * _strlen - main entry
 *
 * @s: pointer to the string
 * @n: the length of the string
 * Return: Always 0(Success)
 */
int _strlen(char *s);
int _strlen(char *s)
{
	return strlen(s);
}
