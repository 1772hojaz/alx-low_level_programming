#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string and a newline
 * @*str: a pointer to the string
 * @s: the string
 *
 * Returns: Nothing
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
